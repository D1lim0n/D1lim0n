getgenv().SecureMode = true

local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

local Window = Rayfield:CreateWindow({
   Name = "Apeirofobia",
   LoadingTitle = "Rayfield Interface",
   LoadingSubtitle = "by D1lim0n",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil,
      FileName = "Big Hub"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = false,
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided",
      FileName = "Key",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"Hello"}
})

local Tab = Window:CreateTab("Esp", "sparkles")
local Section = Tab:CreateSection("Monsters")
Section:Set("Monsters")

local Section = Tab:CreateSection("Exits")
Section:Set("Exits")

local Section = Tab:CreateSection("Keys")
Section:Set("Keys")

local Tab = Window:CreateTab("Player settings", "biceps-flexed")

local Tab = Window:CreateTab("Destroy  UI", "log-out")
local Section = Tab:CreateSection("Destroy UI")
Section:Set("Destroy UI")

Rayfield:LoadConfiguration()
